
<% @locations.each do |location| %>
  <div class='location'>
    <h2><%= location.name %></h2>

    <% location.books.each do |book| %>
      <div class='book'>
        <%= link_to image_tag(book.image_url), book %> 
      </div>
    <% end %>

  </div>
<% end %>

